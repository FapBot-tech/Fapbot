<nav class="navbar navbar-expand-lg bg-body-tertiary px-md-4 sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('index') }}">FapBot</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
            {% if app.user %}
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('index') }}">Mutes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('warning_list') }}">Warnings</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="chatUserDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Chat Users</a>
                        <ul class="dropdown-menu" aria-labelledby="chatUserDropdown">
                            <li>
                                <a class="dropdown-item" href="{{ path('chat_user_index') }}">Lookup</a>
                            </li>
                            {% if mode == 'rocketchat' %}
                            <li>
                                <a class="dropdown-item" href="{{ path('chat_user_online') }}">Online</a>
                            </li>
                            {% endif %}
                            {% if app.user.chatModerator and mode == 'rocketchat' %}
                                <li>
                                    <a class="dropdown-item" href="{{ path('chat_message_lookup') }}">Messages</a>
                                </li>
                            {% endif %}
                            {% if app.user.chatAdmin %}
                                <li>
                                    <a class="dropdown-item" href="{{ path('bulk_search') }}">Bulk search</a>
                                </li>
                            {% endif %}
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('announcements') }}">Announcements</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('guide_content', { identifier: 'guide_index' }) }}">How to/ Guide</a>
                    </li>
                    {% if app.user.chatAdmin or app.user.admin or app.user.superAdmin %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="adminAreaDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Admin area</a>
                            <ul class="dropdown-menu" aria-labelledby="adminAreaDropdown">
                                {% if app.user.admin %}
                                    <li>
                                        <a class="dropdown-item" href="{{ path('user_management') }}">User Management</a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('stats') }}">Statistics</a>
                                    </li>
                                {% endif %}
                                {% if mode == 'rocketchat' %}
                                    <li>
                                        <a class="dropdown-item" href="{{ path('blocked_user') }}">Naughty list</a>
                                    </li>
                                {% endif %}
                                <li>
                                    <a class="dropdown-item" href="{{ path('reason') }}">Prefilled reasons</a>
                                </li>
                                {% if app.user.superAdmin %}
                                    <div class="dropdown-divider"></div>
                                    <h6 class="dropdown-header">Super admin area</h6>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('page_content_index') }}">Pages</a>
                                    </li>
                                    {% if mode == 'rocketchat' %}
                                        <li>
                                            <a class="dropdown-item" href="{{ path('reports') }}">Reports</a>
                                        </li>
                                    {% endif %}
                                    <li>
                                        <a class="dropdown-item" href="{{ path('channel_list') }}">Channel Management</a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('logs') }}">Logs</a>
                                    </li>
                                {% endif %}
                            </ul>
                        </li>
                    {% endif %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="myAccountDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">My Account</a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="myAccountDropdown">
                            <li>
                                <a class="dropdown-item" href="{{ path('user_new_password') }}">Change password</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('user_logout') }}">Logout</a>
                            </li>
                        </ul>
                    </li>

                {% else %}
                    <ul class="navbar-nav">
                    </ul>
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('user_login') }}">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('chat_user_report') }}">Report</a>
                        </li>
                    </ul>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
